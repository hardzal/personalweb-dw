{{#> layout userLoggedIn=userSession}}
  <div class="container">
      {{!-- <main>
      </main> --}}
    <section>
      <div class="project-post">
        <div class="post-header">
          <h2>MY PROJECT</h2>
        </div>
        {{#if userLoggedIn.id}}
        <div class="post-add-button">
          <a href="/project/add">Tambah Projek</a>
        </div>
        {{/if}}
        <div class="main-post" id="post-main">
          {{#each data}}
          <div class="post-item">
            <div class="post-thumbnail">
              <a href="#">
                <img src="{{this.image}}" alt="title" />
              </a>
            </div>
            <div class="post-title">
              <a href="#">
                <h2>{{this.title}}</h2>
              </a>
            </div>
            <div class="post-time">
              <span>Posted at {{this.createdAt}}</span> ||
              <span>Durasi : 1 bulan</span>
            </div>
            <div class="post-summary">
              <p>
                {{this.description}}
              </p>
            </div>
            <div class="post-category">
              <span>js</span>
              <span>nextjs</span>
              <span>nodejs</span>
            </div>
            {{#if ../userLoggedIn.id}}
            <div class="post-button">
              <a href="/project/{{this.id}}/edit" class="button-edit" target="_blank">edit</a>
              <a href="/project/{{this.id}}/detail" class="button-detail">detail</a>
              <form action="/project/{{this.id}}?_method=DELETE" method="post">
                <input type="hidden" name="id" value="{{this.id}}"/>
                <button type="submit" class="button-delete" onclick="return confirm('Anda yakin ingin menghapus ini?')">delete</button>
              </form>
            </div>
            {{/if}}
          </div>
          {{/each}}
        </div>
      </div>
    </section>
    {{!-- <script src="./js/script.js" type="text/javascript"></script> --}}
{{/layout}}